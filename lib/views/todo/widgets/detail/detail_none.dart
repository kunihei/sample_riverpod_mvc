import 'package:flutter/material.dart';

class DetailNone extends StatelessWidget {
  const DetailNone({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('やることが存在しません'));
  }
}
